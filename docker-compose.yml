version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: intervyxa_ai_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: intervyxa_ai
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
